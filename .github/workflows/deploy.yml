# name: Deploy

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: SSH Deploy
#         uses: appleboy/ssh-action@v1.0.3

#         with:
#           host: moiaccount.in
#           username: moiaccou
#           key: ${{ secrets.SSH_KEY }}
#           port: 22

#           script: |
#             whoami
#             pwd

# -------------------------------- New Workflow --------------------------------
# name: Deploy Backend

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: SSH Deploy
#         uses: appleboy/ssh-action@v1.0.3

#         with:
#           host: ${{ secrets.HOST }}
#           username: ${{ secrets.USERNAME }}
#           key: ${{ secrets.SSH_KEY }}
#           port: 22

#           script: |
#             cd ~/sridhar_jeweller_api

#             echo "Before pull"
#             git status

#             git pull origin main

#             echo "After pull"
#             git log --oneline -5

#             npm install

#             npm run build

#             touch tmp/restart.txt




name: Deploy Backend to cPanel (Production)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: 22
          script: |
            echo "🚀 Starting Production Deployment..."

            cd ~/sridhar_jeweller_api

            git pull origin main

            source /home/moiaccou/nodevenv/sridhar_jeweller_api/20/bin/activate

            rm -rf dist node_modules package-lock.json

            npm ci --production=false

            npm run build

            echo "✅ Build Successful"

            # Restart App
            touch /home/moiaccou/nodevenv/sridhar_jeweller_api/20/tmp/restart.txt

            echo "🎉 Deployment Completed!"

